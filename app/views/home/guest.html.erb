<section id="about" data-title="Public<em>Inbox</em>" ng-show="isActiveSection('about')">
  <%= site_about %>
  <p>You must <%= link_to('log in', '#login') %> to continue.</p>
</section>

<%= render(:partial => 'login') %>
<%= render(:partial => 'register') %>
